import '../css/main.css';

// 9By default imported in footer, so no need for document.ready.
// If you have no global JS, set css-only to true in corresponding
// Vite\enqueue_asset in WI\SonxFSE\Assets.php
// (enabled by default in this theme)
