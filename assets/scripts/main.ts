import '../css/main.css';

// 9By default imported in footer, so no need for document.ready.
// If you have no global JS (default),
// css-only should be to true in corresponding
// Vite\enqueue_asset in includes/class-assets.php
